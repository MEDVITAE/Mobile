<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VitaeApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VitaeApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ConfigActivity"
            android:label="ConfigActivity">
        </activity>
        <activity
            android:name=".HistoricoActivity"
            android:label="HistoricoActivity">
        </activity>
        <activity
            android:name=".PerfilActivity"
            android:label="PerfilActivity">
        </activity>
        <activity
            android:name=".RankingActivity"
            android:label="RankingActivity">
        </activity>
        <activity
            android:name=".DetalheHemocentroActivity"
            android:label="DetalheHemocentroActivity">
        </activity>
        <activity
            android:name=".CadastroActivity"
            android:label="CadastroActivity">
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="LoginActivity">
        </activity>
        <activity
            android:name=".QuizActivity"
            android:label="QuizActivity">
        </activity>
    </application>

</manifest>